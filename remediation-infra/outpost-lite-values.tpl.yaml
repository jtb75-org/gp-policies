agent:
  clientId: "${WIZ_OUTPOST_CLIENT_ID}"
  clientSecret: "${WIZ_OUTPOST_CLIENT_SECRET}"
  outpostId: "${WIZ_OUTPOST_ID}"
imagePullSecrets:
- name: wiz-outpost-lite-image-pull
replicaCount: 2
runners:
  rem-aws-ec2-response-001:
    enabled: true
  rem-aws-ec2-response-002:
    enabled: true
  rem-aws-ec2-response-003:
    enabled: true
